Instance variable: Declared inside the class (outside any method) without the static keyword. Every object gets its own copy.

Class variable: Declared inside the class with the static keyword. Shared across all instances of the class.

Local variable: Declared directly inside a method block to hold temporary data.

Parameter: Defined inside the method's parentheses () to accept inputs when the method is called.
